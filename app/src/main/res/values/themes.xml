<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Contacts" parent="Theme.Material3.DayNight.NoActionBar">

        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/primary</item>
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Contacts" parent="Base.Theme.Contacts" />
    <style name="SlideActivityAnimation">
        <item name="android:activityOpenEnterAnimation">@anim/slider_in</item>
        <item name="android:activityOpenExitAnimation">@anim/slide_left</item>
    </style>

</resources>